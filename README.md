# Configuring-Dynamic-Routes-RIP-Router-Information-Protocol-
Routing Information Protocol (RIP) is an older distance-vector protocol using hop count as its metric. It is suited only for small networks due to a 15-hop limit, slow convergence, and high bandwidth use.
